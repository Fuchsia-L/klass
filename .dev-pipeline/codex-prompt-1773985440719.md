You are fixing issues found during code review for Phase 3: 课表解析与本地写入策略.

## Review Feedback

{
  "approved": false,
  "issues": [
    {
      "file": "src/features/schedule/import/whut-import.ts",
      "line": "49-56",
      "severity": "critical",
      "issue": "Timezone-dependent ISO conversion. `buildDateTime` creates a local Date then calls `.toISOString()` which converts to UTC. A class at 08:00 Beijing time becomes `T00:00:00.000Z` in UTC+8 but `T08:00:00.000Z` in UTC. Tests pass only in one timezone, and the stored times will differ per device locale.",
      "suggestion": "Either (a) store times as local ISO strings without the Z suffix (e.g. `2026-02-25T08:00:00`) using manual string formatting instead of `toISOString()`, or (b) always interpret `start_date` and period times in a fixed timezone (Asia/Shanghai) and convert to UTC explicitly. Option (a) is simpler and matches how most calendar apps treat local events. The test helper `toIsoDateTime` has the same bug and would need the same fix."
    },
    {
      "file": "src/features/schedule/import/whut-import.test.ts",
      "line": "6-14",
      "severity": "critical",
      "issue": "Test helper `toIsoDateTime` uses `new Date(...).toISOString()` which is timezone-dependent. Tests will fail on CI or any machine in a different timezone than the developer's.",
      "suggestion": "Fix in tandem with the production code. If using local ISO strings, format them directly without Date object UTC conversion."
    },
    {
      "file": "src/features/schedule/import/contracts.ts",
      "line": "31-40",
      "severity": "major",
      "issue": "`WhutArrangedScheduleItem` uses English field names (`courseName`, `dayOfWeek`, etc.) but the API returns `WhutCourseScheduleItemRaw` with Chinese-abbreviated fields (`kcmc`, `xqj`, etc.). There is no mapping function between them, and the two interfaces will need to stay in sync manually.",
      "suggestion": "Add a `normalizeRawScheduleItem(raw: WhutCourseScheduleItemRaw): WhutArrangedScheduleItem` mapping function (even if the WebView phase will consume it later), or remove the duplication by having the conversion function accept `WhutCourseScheduleItemRaw` directly with a field-mapping step at the top."
    },
    {
      "file": "src/features/schedule/services/events.service.ts",
      "line": "35-45",
      "severity": "minor",
      "issue": "`replaceImportedEvents` re-normalizes `repeat`, `source`, and `is_completed` on events that `convertWhutArrangedListToEvents` already sets to the exact same values. This is defensive but misleading — it suggests the caller might pass events with different values.",
      "suggestion": "Either trust the caller (remove normalization, keep only validation) or document that this function is a general-purpose imported-event replacer that enforces invariants regardless of caller."
    },
    {
      "file": "src/features/schedule/import/whut-import.ts",
      "line": "87-95",
      "severity": "minor",
      "issue": "`createStableImportedEventId` uses a weak hash (`hash * 31 + charCode`) that is collision-prone — different seeds can produce the same 32-bit result. Since the seed includes the full ISO timestamp this is low-risk in practice, but for hundreds of events per semester the probability is non-negligible.",
      "suggestion": "Use `crypto.randomUUID()` (or the existing `generateId()` from `shared/lib/id`) instead of a deterministic hash. Stable IDs are not needed since `replaceImportedEvents` does a full wipe-and-replace, not a diff/merge."
    },
    {
      "file": "src/features/schedule/import/whut-import.ts",
      "line": "128-129",
      "severity": "minor",
      "issue": "`dayOfWeek > 7` validation happens after `getPeriodWindow` — if `dayOfWeek` is invalid, the error message from the section range check might fire first, which is confusing. Also, `dayOfWeek === 7` (Sunday) is allowed but the offset calculation `(dayOfWeek - 1)` would add 6 days from a Monday start, which is correct — just worth a test.",
      "suggestion": "Move the `dayOfWeek` range check before `getPeriodWindow` for clearer error ordering. Consider adding a test for `dayOfWeek: 7` (Sunday)."
    }
  ],
  "unmetCriteria": [
    "Criterion 1 is partially met — the conversion generates correct dates only in the developer's local timezone. In a different timezone the ISO strings will differ, making the output non-deterministic.",
    "Criterion 4 (subscriber notification) — the test asserts `listener` is called once, but the test results section is empty so it's unclear whether tests actually passed."
  ],
  "overallNotes": "The core conversion logic is well-structured and readable — bitmap expansion, section-to-time mapping, and week unrolling are all correct in intent. The persistence strategy (filter out old whut-import, append new) is clean and integrates well with the existing service layer. However, the timezone bug is a showstopper: `new Date(y, m, d, h, min).toISOString()` produces UTC output that varies by machine timezone, making both the stored event times and the tests non-portable. This must be fixed before approval. The secondary concern is the missing raw→friendly field mapping between `WhutCourseScheduleItemRaw` and `WhutArrangedScheduleItem` — without it, the WebView phase will need to add one anyway, and the two interfaces could drift apart."
}

## Instructions

1. Fix each issue listed above
2. Pay special attention to critical and major issues
3. Re-run all tests after fixing to ensure nothing is broken
4. Do NOT introduce new features — only fix the reported issues
5. If a suggestion doesn't make sense, use your judgment but still address the underlying concern
